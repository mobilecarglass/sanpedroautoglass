
document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".nav"),back=document.querySelector(".nav-backdrop");const close=()=>{if(!nav)return;nav.classList.remove("open");back?.classList.remove("open");toggle?.setAttribute("aria-expanded","false");document.body.style.overflow=""};const open=()=>{nav.classList.add("open");back?.classList.add("open");toggle?.setAttribute("aria-expanded","true");document.body.style.overflow="hidden"};toggle?.addEventListener("click",()=>nav.classList.contains("open")?close():open());back?.addEventListener("click",close);nav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",close));document.addEventListener("keydown",e=>{if(e.key==="Escape")close()});
document.querySelectorAll(".carousel").forEach(car=>{const items=[...car.querySelectorAll(".carousel-inner .item")],dots=[...car.querySelectorAll(".carousel-indicators li")];if(items.length<2)return;let i=items.findIndex(x=>x.classList.contains("active"));if(i<0)i=0;const show=n=>{i=(n+items.length)%items.length;items.forEach((x,j)=>x.classList.toggle("active",j===i));dots.forEach((x,j)=>x.classList.toggle("active",j===i))};dots.forEach((d,j)=>d.addEventListener("click",()=>show(j)));car.querySelector(".left")?.addEventListener("click",e=>{e.preventDefault();show(i-1)});car.querySelector(".right")?.addEventListener("click",e=>{e.preventDefault();show(i+1)});setInterval(()=>show(i+1),6000)});});
